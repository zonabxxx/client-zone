---
interface Props {
  active: 'orders' | 'quotes';
}

const { active } = Astro.props;

const navItems = [
  { id: 'orders', label: 'Zákazky', labelFull: 'Aktívne zákazky', href: '/dashboard', icon: '📦' },
  { id: 'quotes', label: 'Ponuky', labelFull: 'Cenové ponuky', href: '/quotes', icon: '💰' },
];
---

<nav class="flex gap-2 mb-6 sm:mb-8">
  {navItems.map((item) => (
    <a 
      href={item.href}
      class:list={[
        "flex items-center justify-center gap-1.5 sm:gap-2 px-3 sm:px-5 py-2.5 sm:py-3 rounded-lg sm:rounded-xl text-xs sm:text-sm font-medium transition-all flex-1 sm:flex-none",
        active === item.id 
          ? "bg-gradient-to-r from-primary-600 to-primary-500 text-white shadow-lg shadow-primary-600/20" 
          : "glass-light text-slate-300 hover:text-white hover:bg-white/10"
      ]}
    >
      <span class="text-base sm:text-lg">{item.icon}</span>
      <span class="sm:hidden">{item.label}</span>
      <span class="hidden sm:inline">{item.labelFull}</span>
    </a>
  ))}
</nav>
